window.FormenWebGL=window.FormenWebGL||{};(()=>{if(typeof THREE==='undefined'||matchMedia('(prefers-reduced-motion:reduce)').matches)return;const host=document.querySelector('#webgl-bg');if(!host)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,100);camera.position.z=5;const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);host.appendChild(renderer.domElement);FormenWebGL.scene=scene;FormenWebGL.camera=camera;FormenWebGL.renderer=renderer;window.dispatchEvent(new Event('formen:webgl-ready'));})();